name: Build and Push Docker Image

on:
  workflow_call:

jobs:
  build-scan-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Image
        run: |
          docker build -t cloud-notes:${{ github.sha }} .

      - name: Scan Image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: cloud-notes:${{ github.sha }}
          format: table
          exit-code: '1'
          severity: CRITICAL,HIGH

      - name: Tag Image
        run: |
          docker tag cloud-notes:${{ github.sha }} ${{ vars.DOCKERHUB_USERNAME }}/cloud-notes:latest
          docker tag cloud-notes:${{ github.sha }} ${{ vars.DOCKERHUB_USERNAME }}/cloud-notes:${{ github.ref_name }}
          docker tag cloud-notes:${{ github.sha }} ${{ vars.DOCKERHUB_USERNAME }}/cloud-notes:${{ github.sha }}

      - name: Push Image
        run: |
          docker push ${{ vars.DOCKERHUB_USERNAME }}/cloud-notes:latest
          docker push ${{ vars.DOCKERHUB_USERNAME }}/cloud-notes:${{ github.ref_name }}
          docker push ${{ vars.DOCKERHUB_USERNAME }}/cloud-notes:${{ github.sha }}