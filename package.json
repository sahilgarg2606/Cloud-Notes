{
  "name": "notes-manager",
  "version": "1.0.0",
  "description": "A full-fledged Notes Manager web app for DevOps practices.",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "node --watch src/app.js",
    "test": "echo \"Error: no test specified\" && exit 0",
     "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}